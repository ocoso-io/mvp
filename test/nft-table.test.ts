import { expect } from '@open-wc/testing';

// Einfacher Test ohne Komponenten-Import zum Testen der Setup-Umgebung
describe('Basic test setup', () => {
  it('should work', () => {
    expect(true).to.be.true;
  });
});