import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { Card, CardContent } from '@/components/ui/card'; // Falls diese Komponenten verfügbar sind
import { Tooltip } from '@/components/ui/tooltip';       // Andernfalls kannst Du auch ein einfaches DIV als Tooltip verwenden

const UploadArea = () => {
  const [isHovered, setIsHovered] = useState(false);

  return (
    <div
      className="upload-area"
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        padding: '20px',
        border: '3px dashed #ccc',
        borderRadius: '20px',
        margin: '0 auto',
      }}
    >
      <div className="upload-content" style={{ textAlign: 'center', marginBottom: '20px' }}>
        <h2>Drop it! it's hot!!</h2>
        <p>JPEG, PNG, MP4 ... Just drag it in or click to download.</p>
      </div>

      <Card
        className="relative"
        style={{ width: '100%', maxWidth: '600px' }}
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <CardContent className="flex flex-col items-center justify-center h-64 cursor-pointer">
          <motion.div animate={{ scale: isHovered ? 1.05 : 1 }} transition={{ duration: 0.2 }}>
            <div className="text-3xl font-bold">📂 Drag your files here</div>
          </motion.div>
          {isHovered && (
            <Tooltip>
              <div
                className="absolute top-0 left-1/2 transform -translate-x-1/2 mt-4 px-4 py-2 bg-yellow-500 text-black rounded-full shadow-md"
                style={{ whiteSpace: 'nowrap' }}
              >
                Yeah, just drop it like it’s hot!
              </div>
            </Tooltip>
          )}
        </CardContent>
      </Card>

      <button
        className="content-button"
        aria-label="Content navigation button"
        style={{
          width: '300px',
          margin: '20px 40px 0 -23px',
          borderRadius: '0 0 20px 20px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '10px',
          border: 'none',
          backgroundColor: '#f8f9fa',
          cursor: 'pointer',
          transition: 'transform 0.2s ease',
        }}
      >
        <img
          src="img/Arrow-black.png"
          alt="Navigation icon"
          className="content-button-icon"
          style={{ width: '40px', height: '40px' }}
        />
        <span className="content-button-text" style={{ marginLeft: '10px' }}>
          Take it ...
        </span>
      </button>
    </div>
  );
};

export default UploadArea;