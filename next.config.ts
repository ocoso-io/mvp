/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Optionale Anpassungen
    // transpilePackages: ['effect'], // Falls Sie effect.js in Next.js verwenden möchten
    // distDir: 'dist/next', // Anpassen des Build-Verzeichnisses falls gewünscht
};

module.exports = nextConfig;